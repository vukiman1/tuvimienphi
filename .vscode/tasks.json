{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build backend development",
      "type": "shell",
      "command": "pnpm nx run @org/backend:build:development",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
