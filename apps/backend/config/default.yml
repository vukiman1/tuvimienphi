# Values that hold everywhere. Anything machine-specific (localhost hosts, dev credentials) lives
# in local.yml; production takes those from environment variables instead.

app:
  name: Tu Vi Mien Phi
  port: 3000
  nodeEnv: development
  trustProxy: 0
  logLevels:
    - error
    - warn
    - log
    - debug
    - verbose

db:
  replicas: []
  log:
    enabled: true

redis:
  cluster: false
  db: 0

sentry:
  dsn: ''
  tracesSampleRate: 0.1

jwt:
  accessTokenExpiresIn: 15m

# Gói free của Google chỉ mở một phần model và model mới thường quá tải, nên phải có danh sách
# để thử lần lượt. Xếp model đã đo là dùng được lên đầu.
ai:
  geminiApiKey: ''
  models:
    - gemini-3.5-flash-lite
    - gemini-3.8-flash
  temperature: 0.7

email:
  resendApiKey: ''
  from: Tu Vi Mien Phi <onboarding@resend.dev>

session:
  maxSessionsPerUser: 5
  refreshTtl: 1d
  refreshTtlRemember: 60d
  refreshTtlOauth: 30d

captcha:
  enabled: false
  secretKey: ''

google:
  clientId: ''

queueBoard:
  enabled: true
  user: admin
  password: ''
