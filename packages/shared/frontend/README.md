# frontend-shared

Utilities shared by the browser apps (`@org/frontend`, `@org/dashboard`): the `cn` class-name
merger, the `ApiError` family for backend failure envelopes, and the React Query client factory.

Import from `@org/frontend-shared`.

## Running unit tests

Run `nx test frontend-shared` to execute the unit tests via [Jest](https://jestjs.io).
